\section{Empirical Experiments}
\label{sec:experiments}

We evaluate the two claims made in the introduction: LARA should improve generation, and its internal representations should become stronger. The main experiments are organized around two questions:
\begin{itemize}[leftmargin=*]
    \item Does LARA improve generation quality across domains and model sizes? (\Cref{sec:image_generation,sec:human_motion_generation})
    \item Does LARA improve the backbone representations it supervises? (\Cref{sec:representation_analysis})
\end{itemize}
The main tables keep comparisons compact. Training settings, sampling protocols, evaluator details, and sensitivity sweeps are reported in \Cref{app:experimental_details,app:realign_ablation_protocols,app:metric_definitions}. Objective and sampling choices are isolated in \Cref{sec:ablation_study}.

\subsection{Image Generation}
\label{sec:image_generation}
\label{sec:experimental_setup}

\paragraph{Implementation details.}
Unless otherwise stated, we follow the class-conditional ImageNet training setup of SiT~\citep{ma2024sit}. We train on ImageNet~\citep{deng2009imagenet} at $256\times256$ resolution using the preprocessing protocol of ADM~\citep{dhariwal2021diffusion}. Each image is encoded by the Stable Diffusion VAE from LDM~\citep{rombach2022ldm} into a latent tensor $z\in\mathbb{R}^{32\times32\times4}$. We evaluate the \texttt{B/2}, \texttt{L/2}, and \texttt{XL/2} SiT configurations with patch size $2$. LARA trains the transition predictor jointly with the backbone using \Cref{eq:total_loss}. Unless specified, we use AdamW with learning rate $10^{-4}$ and batch size $256$. Additional details on architecture, transition-pair sampling, EMA decay, loss weights, training length, hardware, and compute budget are organized in \Cref{app:experimental_details}.

\paragraph{Evaluation.}
We report Frechet Inception Distance (FID)~\citep{heusel2017gans}, Inception Score (IS)~\citep{salimans2016improved}, precision, and recall~\citep{kynkaanniemi2019precision} using $50\mathrm{K}$ generated samples. We use the same reference statistics and evaluation implementation for all LARA variants and baselines. Definitions for the diagnostic metrics that are not standard generation benchmarks are provided in \Cref{app:metric_definitions}.

\paragraph{Comparison methods.}
Our main comparisons are against the base SiT backbone and recent self-contained representation-regularized methods: Dispersive Loss~\citep{wang2025dispersive}, SRA~\citep{jiang2025sra}, and LayerSync~\citep{haghighi2026layersync}. For system-level context, we also report representative ImageNet baselines from pixel diffusion~\citep{dhariwal2021diffusion,kingma2023understanding,hoogeboom2023simple,ho2022cascaded}, latent diffusion~\citep{rombach2022ldm}, transformer-hybrid diffusion~\citep{bao2023all,hatamizadeh2024diffit,gao2023mdtv2}, and autoregressive or predictive generation~\citep{tian2024visual,chen2024jepa}. Detailed baseline descriptions and the parameter-matching protocol are deferred to \Cref{app:baseline_descriptions,app:baseline_protocol}.

\paragraph{Results.}
\Cref{tab:imagenet_no_cfg} reports the main no-CFG ImageNet $256\times256$ comparison under matched SiT training settings, while \Cref{tab:imagenet_cfg} reports a system-level comparison with CFG. The primary comparison is within each backbone and training budget; this separates the effect of LARA from changes in model size, sampler, or guidance.


\begin{table}[!htbp]
    \centering
    \begin{minipage}[t]{0.49\textwidth}
    \captionsetup{font=small,justification=raggedright,singlelinecheck=false}
    \captionof{table}{\textbf{Class-conditional generation on ImageNet $256\times256$.} No classifier-free guidance (CFG)~\citep{ho2022classifier} is used.}
    \label{tab:imagenet_no_cfg}
    \vspace{0.35em}
    \scriptsize
    \setlength{\tabcolsep}{3.0pt}
    \renewcommand{\arraystretch}{0.9}
    \centering
    \begin{tabular}{@{}lccc@{}}
        \toprule
        Model & \#Params & Iter. & FID$\downarrow$ \\
        \midrule
        \texttt{SiT-B/2} & 130M & 400K & 35.17 \\
        \quad + Dispersive & 130M & 400K & 31.27 \\
        \quad + SRA & 130M & 400K & 30.84 \\
        \quad + LayerSync & 130M & 400K & 28.63 \\
        \quad + LARA (ours) & 130M & 400K & \textbf{25.01} \\
        \midrule
        \texttt{SiT-L/2} & 458M & 400K & 20.55 \\
        \quad + Dispersive & 458M & 400K & 16.27 \\
        \quad + SRA & 458M & 400K & 15.50 \\
        \quad + LayerSync & 458M & 400K & 13.04 \\
        \quad + LARA (ours) & 458M & 400K & \textbf{11.09} \\
        \midrule
        \texttt{SiT-XL/2} & 675M & 400K & 17.32 \\
        \quad + Dispersive & 675M & 400K & 15.35 \\
        \quad + SRA & 675M & 400K & 15.26 \\
        \quad + LayerSync & 675M & 400K & 11.06 \\
        \quad + LARA (ours) & 675M & 400K & \textbf{8.57} \\
        \midrule
        \texttt{SiT-XL/2} & 675M & 1M & 12.03 \\
        \quad + Dispersive & 675M & 1M & 10.62 \\
        \quad + SRA & 675M & 1M & 10.59 \\
        \quad + LayerSync & 675M & 1M & 8.12 \\
        \quad + LARA (ours) & 675M & 1M & \textbf{6.51} \\
        \midrule
        \texttt{SiT-XL/2} & 675M & 2M & 8.85 \\
        \quad + Dispersive & 675M & 2M & 8.14 \\
        \quad + SRA & 675M & 2M & 7.97 \\
        \quad + LayerSync & 675M & 2M & 6.80 \\
        \quad + LARA (ours) & 675M & 2M & \textbf{5.09} \\
        \bottomrule
    \end{tabular}
\end{minipage}
    \hfill
    \begin{minipage}[t]{0.47\textwidth}
        \captionsetup{font=small,justification=raggedright,singlelinecheck=false}
        \captionof{table}{\textbf{System-level comparison on ImageNet $256\times256$ with CFG~\citep{ho2022classifier}.}}
        \label{tab:imagenet_cfg}
        \vspace{0.35em}
        \scriptsize
        \setlength{\tabcolsep}{2.5pt}
        \renewcommand{\arraystretch}{0.95}
        \begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}lccccc@{}}
            \toprule
            Model & Iter. & FID$\downarrow$ & IS$\uparrow$ & Pre.$\uparrow$ & Rec.$\uparrow$ \\
            \midrule
            \multicolumn{6}{@{}l}{\textit{Pixel diffusion}} \\
            ADM-U & 2.00M & 3.94 & 186.7 & 0.82 & 0.52 \\
            VDM++ & 2.80M & 2.40 & 225.3 & -- & -- \\
            Simple diffusion & 4.00M & 2.77 & 211.8 & -- & -- \\
            CDM & 10.80M & 4.88 & 158.7 & -- & -- \\
            \midrule
            \multicolumn{6}{@{}l}{\textit{Autoregressive image generation}} \\
            VAR-d20 & 1.75M & 2.57 & 302.6 & 0.83 & 0.56 \\
            D-JEPA-L & 2.40M & \textbf{1.58} & 303.1 & 0.80 & 0.61 \\
            \midrule
            \multicolumn{6}{@{}l}{\textit{Latent diffusion, U-Net}} \\
            LDM-4 & 1.00M & 3.60 & 247.7 & \textbf{0.87} & 0.48 \\
            \midrule
            \multicolumn{6}{@{}l}{\textit{Latent diffusion, Transformer + U-Net hybrid}} \\
            U-ViT-H/2 & 1.20M & 2.29 & 263.9 & 0.82 & 0.57 \\
            DiffiT & -- & 1.73 & 276.5 & 0.80 & 0.62 \\
            MDTv2-XL/2 & 5.40M & \textbf{1.58} & \textbf{314.7} & 0.79 & \textbf{0.65} \\
            \midrule
            \multicolumn{6}{@{}l}{\textit{Latent diffusion, Transformer}} \\
            DiT-XL/2 & 7.00M & 2.58 & 278.2 & 0.83 & 0.57 \\
            SiT-XL/2 & 7.00M & 2.26 & 270.3 & 0.82 & 0.59 \\
            \quad + REPA & 2.00M & \textbf{1.58} & 284.0 & 0.81 & 0.61 \\
            \quad + SRA & 2.00M & 1.93 & 303.4 & 0.74 & 0.59 \\
            \hdashline
            \noalign{\vskip 0.5ex}
            \quad + Dispersive & 2.00M & 1.97 & -- & -- & -- \\
            \quad + LayerSync & 2.00M & 1.87 & 265.34 & 0.81 & 0.60 \\
            \quad + LARA (ours) & 2.00M & 1.81 & 276.48 & 0.81 & 0.61 \\
            \bottomrule
        \end{tabular*}
    \end{minipage}
\end{table}

The no-CFG results show the same pattern across available scales. On \texttt{SiT-B/2}, LARA improves FID from $35.17$ to $25.01$, a $28.9\%$ relative gain over the base model and a $12.6\%$ gain over LayerSync. On \texttt{SiT-XL/2} at $400\mathrm{K}$ iterations, LARA improves FID from $17.32$ to $9.75$, a $43.7\%$ gain over the base model and an $11.8\%$ gain over LayerSync. The \texttt{SiT-L/2}, \texttt{SiT-XL/2} $1\mathrm{M}$, and \texttt{SiT-XL/2} $2\mathrm{M}$ LARA runs are reported as \textbf{[TBD]} in the table.

\subsection{Text-Conditional Human Motion Generation}
\label{sec:human_motion_generation}

To test whether LARA transfers beyond image generation, we additionally evaluate text-conditioned human motion generation on HumanML3D~\citep{guo2022generating}. Given a text description, the model generates a sequence of human poses represented as 3D joint trajectories.

\paragraph{Implementation details.}
We follow the MDM setup~\citep{tevet2022mdm} and use a transformer backbone with the same training budget for the baseline and LARA variant. The main comparison trains MDM with and without LARA for $600\mathrm{K}$ iterations; architecture and loss settings are reported in \Cref{app:realign_hyperparameters}.

\paragraph{Evaluation metrics.}
We report FID and R-Precision (top 3) using the standard HumanML3D evaluation protocol. Definitions and reporting conventions are provided in \Cref{app:metric_definitions}. Lower FID is better, while higher R-Precision indicates better text-motion retrieval alignment.

\paragraph{Results.}
\Cref{tab:humanml3d_results} reports the HumanML3D comparison. This experiment tests whether the same signal-extracting transition objective transfers to a smaller, non-image transformer without changing the generative task.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Quantitative results for text-conditional human motion generation on HumanML3D.}}
    \label{tab:humanml3d_results}
    \small
    \setlength{\tabcolsep}{8pt}
    \renewcommand{\arraystretch}{0.9}
    \begin{tabular}{@{}lccc@{}}
        \toprule
        Method & Iter. & FID $\downarrow$ & R-Precision $\uparrow$ \\
        \midrule
        MDM & 600K & \textbf{[TBD]} & \textbf{[TBD]} \\
        + LayerSync & 600K & \textbf{[TBD]} & \textbf{[TBD]} \\
        + LARA (Ours) & 600K & \textbf{[TBD]} & \textbf{[TBD]} \\
        \bottomrule
    \end{tabular}
\end{table}

\FloatBarrier

\subsection{Representation Analysis}
\label{sec:representation_analysis}

Generation metrics show the final effect of training. LARA, however, is designed to act inside the depth hierarchy, so we also inspect hidden states directly. We compare matched-FID checkpoints: an \texttt{SiT-XL/2} trained with LARA for \textbf{[TBD]} iterations and a baseline \texttt{SiT-XL/2} trained for \textbf{[TBD]} iterations. Matching generation quality makes the comparison more direct: differences in the probes reflect how the regularizer changes the learned representations, rather than simply reflecting a stronger final generator.

We probe every backbone block with three layerwise diagnostics: linear classification on Tiny ImageNet~\citep{deng2009imagenet}, linear segmentation on PASCAL VOC~\citep{everingham2010pascal}, and Centered Kernel Alignment (CKA) to DINOv2 features~\citep{kornblith2019similarity,oquab2023dinov2}. The two linear probes measure how much semantic and spatial information is exposed by each layer, while CKA measures whether the hidden states organize visual structure similarly to a strong visual representation. Probe heads, layerwise reporting conventions, and additional diagnostics are reported in \Cref{app:layer_statistics_projection,app:metric_definitions}.

\begin{figure}[!htbp]
    \centering
    \fbox{%
    \begin{minipage}[c][0.24\textheight][c]{0.92\linewidth}
        \centering
        \small
        \textbf{[PLACEHOLDER: layerwise representation analysis]}\\[0.5em]
        Panels: Tiny ImageNet linear probe, PASCAL VOC segmentation probe, DINOv2 CKA,\\
        and optional PCA/layout visualization across baseline, LayerSync, and LARA.
    \end{minipage}}
    \caption{\textbf{Layerwise representation quality.} Placeholder for probes across backbone depth under matched generation settings. Stronger weak- and middle-layer curves indicate that cross-depth transition supervision makes useful features easier to recover throughout the backbone.}
    \label{fig:representation_analysis}
\end{figure}

\Cref{fig:representation_analysis} summarizes the layerwise probes and suggests two main trends. First, LARA distributes useful features more evenly across depth: averaged over the evaluated layers, linear classification improves by \textbf{[TBD]\%}, segmentation mIoU improves by \textbf{[TBD]\%}, and CKA alignment with DINOv2 improves by \textbf{[TBD]\%}. This is consistent with the role of the transition predictor: supervision is no longer concentrated around one selected pair, but reused across many source--target depths. Second, the best layer also improves, with peak linear accuracy increasing by \textbf{[TBD]\%}. This is less automatic than raising weak layers toward a fixed reference. It indicates that shared transition learning can reorganize the feature hierarchy, producing a stronger representation rather than merely copying one high-performing block.

This pattern is the internal trace expected from LARA. Weak layers expose cleaner signals, middle layers become stronger targets, and the gains spread across the hierarchy instead of collapsing around a hand-picked reference layer.

\FloatBarrier

\section{Ablation Study}
\label{sec:ablation_study}

We isolate the main design choices before validating the final recipe in the generation tables. The ablations test the objective terms, the cosine-squared regularizer, and the transition errors induced by uniform layer-pair sampling. Detailed settings and extended controls are reported in \Cref{app:realign_ablation_protocols}.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Objective ablation on SiT-B/2.} All rows use the same TPU training budget; FID is computed with $10\mathrm{K}$ samples. The table isolates transition prediction, transition consistency, and the cosine-squared diversity regularizer.}
    \label{tab:realign_ablation}
    \small
    \setlength{\tabcolsep}{4pt}
    \renewcommand{\arraystretch}{0.88}
    \begin{tabular*}{0.90\linewidth}{@{\extracolsep{\fill}}lcccc@{}}
        \toprule
        Variant & $\mathcal{L}_{\mathrm{pred}}$ & $\mathcal{L}_{\mathrm{cons}}$ & $\cos^2$ & FID-10K $\downarrow$ \\
        \midrule
        Vanilla & -- & -- & -- & \textbf{[TBD]} \\
        LayerSync & -- & -- & -- & \textbf{[TBD]} \\
        Cosine squared only & -- & -- & \checkmark & \textbf{[TBD]} \\
        LARA pred. & \checkmark & -- & -- & \textbf{[TBD]} \\
        LARA pred. + cons. & \checkmark & \checkmark & -- & \textbf{[TBD]} \\
        Full LARA & \checkmark & \checkmark & \checkmark & \textbf{[TBD]} \\
        \bottomrule
    \end{tabular*}
\end{table}

\begin{figure}[!htbp]
    \centering
    \fbox{%
    \begin{minipage}[c][0.22\textheight][c]{0.92\linewidth}
        \centering
        \small
        \textbf{[PLACEHOLDER: layer cosine heatmaps]}\\[0.5em]
        Left: cosine-similarity heatmap across backbone layers before $\cos^2$ regularization.\\
        Right: the same heatmap after applying the cosine-squared term.
    \end{minipage}}
    \caption{\textbf{Effect of the cosine-squared regularizer.} The heatmaps visualize pairwise layer similarity before and after the regularizer. The intended effect is simple: reduce excessive layer collapse while preserving the cross-depth transition signal.}
    \label{fig:cosine_squared_heatmap}
\end{figure}

Uniform layer-pair training is both a baseline and a diagnostic. A useful shared predictor should already work under uniform sampling; its prediction errors then reveal which transitions are too easy, too hard, or most informative. The exact diagnostic protocol is reported in \Cref{app:realign_ablation_protocols}.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Where uniform transition prediction is difficult.} Prediction error is measured under uniform pair sampling. These diagnostics motivate the final pair-selection strategy; full schedule details are in \Cref{app:realign_ablation_protocols}.}
    \label{tab:pair_sampling_ablation}
    \scriptsize
    \setlength{\tabcolsep}{2.2pt}
    \renewcommand{\arraystretch}{0.88}
    \begin{minipage}[t]{0.32\linewidth}
        \centering
        \textbf{Layer gap}\\[0.25em]
        \begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}lcc@{}}
            \toprule
            Gap & Share & $\mathcal{L}_{\mathrm{pred}}\downarrow$ \\
            \midrule
            Short & \textbf{[TBD]} & \textbf{[TBD]} \\
            Medium & \textbf{[TBD]} & \textbf{[TBD]} \\
            Long & \textbf{[TBD]} & \textbf{[TBD]} \\
            Very long & \textbf{[TBD]} & \textbf{[TBD]} \\
            \bottomrule
        \end{tabular*}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.32\linewidth}
        \centering
        \textbf{Source layer}\\[0.25em]
        \begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}lcc@{}}
            \toprule
            Source & Share & $\mathcal{L}_{\mathrm{pred}}\downarrow$ \\
            \midrule
            Early & \textbf{[TBD]} & \textbf{[TBD]} \\
            Early-mid & \textbf{[TBD]} & \textbf{[TBD]} \\
            Middle & \textbf{[TBD]} & \textbf{[TBD]} \\
            Late & \textbf{[TBD]} & \textbf{[TBD]} \\
            \bottomrule
        \end{tabular*}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.32\linewidth}
        \centering
        \textbf{Timestep}\\[0.25em]
        \begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}lcc@{}}
            \toprule
            Range & Share & $\mathcal{L}_{\mathrm{pred}}\downarrow$ \\
            \midrule
            High noise & \textbf{[TBD]} & \textbf{[TBD]} \\
            Mid noise & \textbf{[TBD]} & \textbf{[TBD]} \\
            Low noise & \textbf{[TBD]} & \textbf{[TBD]} \\
            All & 1.00 & \textbf{[TBD]} \\
            \bottomrule
        \end{tabular*}
    \end{minipage}
\end{table}

These tables lead to a simple strategy: keep enough uniform coverage for layer-agnostic alignment, then bias training toward transitions that are informative and learnable. The final schedule mixes medium layer gaps, avoids over-sampling trivial neighboring layers, and reweights timesteps where transition prediction carries the clearest signal. Additional controls for max gap, timestep schedule, predictor architecture, loss weights, PCA diagnostics, qualitative samples, and standard-versus-interface inference are in \Cref{app:realign_ablation_protocols,app:layer_statistics_projection,app:qualitative_examples}.

Overall, the experiments indicate that LARA's gains are not simply a consequence of choosing a better weak--strong pair. LayerSync shows that a selected internal alignment can regularize training, but its supervision remains tied to one hand-picked relation. LARA uses the same internal hierarchy more broadly: the shared transition predictor learns from many source--target pairs, while layer conditioning preserves the pair-specific gaps. This gives the model a more global alignment signal across depth, rather than a heuristic constraint anchored to a single reference layer.

\FloatBarrier
