\section{LARA Method Details}
\label{app:realign_algorithm_compute}

This appendix keeps the main paper compact while making the implementation easy to audit. We organize the details into four groups: LARA method details, experimental protocols, additional ablations, and representation diagnostics. Cross-references from the main text point to the specific subsection needed for each result.

\subsection{Training Algorithm and Compute}

LARA adds a lightweight training-time branch to the diffusion transformer. The backbone forward pass exposes source and target activations, the shared predictor maps the source state into target-layer coordinates, and a small depth-shortcut subset can be routed through the predicted state inside the same flow-matching objective. The auxiliary regularizer combines transition prediction, path consistency, and weak-layer diversity.

\begin{algorithm}[!htbp]
\caption{\textbf{LARA training.} Flow matching with an integrated depth shortcut.}
\label{alg:realign_training}
\small
\begin{algorithmic}[1]
\Require backbone $v_\theta$ with prefix/suffix maps $F_{\theta,1:a},F_{\theta,b:L}$; online predictor $G_\psi$ and EMA predictor $G_{\bar\psi}$.
\Require timestep law $p_t$; pair law $q(a,b,c)$; depth-shortcut subset $\mathcal{B}_{\mathrm{ds}}$; weights $\lambda_{\mathrm{pred}},\lambda_{\mathrm{cons}},\lambda_{\mathrm{div}}$.
\Repeat
    \State Sample $x_0\sim p_{\mathrm{data}}$, condition $y\sim p(y\mid x_0)$, noise $\epsilon\sim\mathcal{N}(0,I)$, timestep $t\sim p_t$.
    \State Form $x_t=\alpha_t x_0+\sigma_t\epsilon$ and $u_t=\dot\alpha_t x_0+\dot\sigma_t\epsilon$.
    \State Run the full backbone and cache hidden states:
    \[
    (\hat v,\mathcal{H})\leftarrow v_\theta(x_t,t,y),\qquad
    \mathcal{H}=\{A_\ell^t\}_{\ell=0}^{L}.
    \]
    \State Sample $(a,b,c)\sim q(a,b,c)$ with $a<b<c$, and compute transitions from cached states:
    \[
    \begin{aligned}
    \hat A_{a\to b}^t &= G_\psi(A_a^t;e_a,e_b,e_t),\\
    \hat A_{a\to c}^t &= G_\psi(\mathrm{sg}(A_a^t);e_a,e_c,e_t),\\
    \tilde A_{a\to b\to c}^t
    &= G_{\bar\psi}\!\left(\mathrm{sg}(\hat A_{a\to b}^t);e_b,e_c,e_t\right).
    \end{aligned}
    \]
    \Statex \hspace{\algorithmicindent}\textit{This uses two online predictor forwards and one EMA predictor forward; the inner $a\to b$ state is cached, not recomputed.}
    \State Compute the routed velocity for the depth-shortcut subset:
    \[
    \tilde v=F_{\theta,b:L}(\hat A_{a\to b}^t,t,y).
    \]
    \State Compute the unified flow-matching loss:
    \[
    \mathcal{L}_{\mathrm{FM}}
    =
    \mathbb{E}_{i\in\mathcal{B}}
    \left[
    \|\hat v_i-u_{t,i}\|_2^2
    +
    \mathbb{I}_{\{i\in\mathcal{B}_{\mathrm{ds}}\}}
    \|\tilde v_i-u_{t,i}\|_2^2
    \right].
    \]
    \State Compute the LARA regularizer from the cached states in $\mathcal{H}$:
    \[
    \begin{aligned}
    \mathcal{L}_{\mathrm{LARA}}
    &=
    \lambda_{\mathrm{pred}}d(\hat A_{a\to b}^t,\mathrm{sg}(A_b^t))\\
    &\quad+\lambda_{\mathrm{cons}}d(\hat A_{a\to c}^t,\mathrm{sg}(\tilde A_{a\to b\to c}^t))\\
    &\quad+\lambda_{\mathrm{div}}\mathbb{E}_{i<j}
    \operatorname{cos}^2(A_i^t,A_j^t),
    \qquad A_i^t,A_j^t\in\mathcal{H}.
    \end{aligned}
    \]
    \State Update $\theta,\psi$ with $\mathcal{L}_{\mathrm{FM}}+\mathcal{L}_{\mathrm{LARA}}$.
    \State Update the EMA predictor $\bar\psi\leftarrow \mu\bar\psi+(1-\mu)\psi$.
\Until{training budget is reached}
\end{algorithmic}
\end{algorithm}

The timestep law $p_t$ is a protocol choice rather than part of the transition objective. Main LARA runs use the default training sampler reported in \Cref{tab:appendix_imagenet_protocol}; timestep-sampler alternatives are evaluated separately in \Cref{tab:main_hyperparameter_tuning}.

\paragraph{Predictor-induced transition distance.}
The distance term is evaluated on the transition produced by LARA, not on the two raw hidden states. Given a source--target pair $(a,b)$, the source activation is first passed through the layer-aware predictor so that the comparison is made in the target-layer coordinate system. The target activation is used only as a fixed reference, and the final score averages the tokenwise directional mismatch.

\begin{algorithm}[!htbp]
\caption{\textbf{Predictor-induced transition distance in LARA.}}
\label{alg:lara_transition_distance}
\small
\begin{algorithmic}[1]
\Require source state $A_a^t$; target state $A_b^t$; transition predictor $G_\psi$; conditioning tuple $(e_a,e_b,e_t)$.
\State $\widehat A_{a\to b}^{\,t}\gets G_\psi(A_a^t;e_a,e_b,e_t)$
\State $R_b^t\gets \operatorname{sg}(A_b^t)$
\For{$n=1,\ldots,N$}
    \State $s_n\gets \operatorname{cos\_sim}(\widehat A_{a\to b,n}^{\,t},R_{b,n}^{t})$
    \State $\delta_n\gets 1-s_n$
\EndFor
\State $d\gets \frac{1}{N}\sum_{n=1}^{N}\delta_n$
\State \Return $d$
\end{algorithmic}
\end{algorithm}

Equivalently, the transition distance used in \Cref{alg:realign_training} is
\begin{equation}
d(\widehat A_{a\to b}^{\,t},\operatorname{sg}(A_b^t))
=
\frac{1}{N}\sum_{n=1}^{N}
\left(
1-
\operatorname{cos\_sim}\left(\widehat A_{a\to b,n}^{\,t},\operatorname{sg}(A_{b,n}^{t})\right)
\right).
\label{eq:appendix_token_cosine_distance}
\end{equation}
The diversity term is computed directly from the hidden-state cache $\mathcal{H}$ and does not require an additional backbone forward pass.

\subsection{Training Compute}
\label{app:training_compute}

\begin{table}[H]
    \centering
    \caption{\textbf{Training compute.} Placeholder table for measuring per-step training cost on \texttt{SiT-XL/2} for all three methods, under the same input resolution, batch size, precision, hardware, and step count. TFLOPs include forward and backward passes and exclude data loading and cached VAE encoding.}
    \label{tab:appendix_compute_overhead}
    \scriptsize
    \setlength{\tabcolsep}{6pt}
    \begin{tabular*}{0.58\textwidth}{@{\extracolsep{\fill}}lc@{}}
        \toprule
        Method & Train TFLOPs/step \\
        \midrule
        SiT baseline & \textbf{0.23 | 30,305} \\
        LayerSync & \textbf{0.25 | 30,305} \\
        LARA (Ours) & \textbf0.30 | {35,342} \\
        \bottomrule
    \end{tabular*}
\end{table}

\subsection{Compute Comparison with SRA and LayerSync}
\label{app:sra_layersync_lara_compute}

We additionally profile the internal alignment methods used in our speed analysis. The matched compute comparison uses our \texttt{SiT-XL/2} setup on TPU v4-16 with the same input resolution, precision, batch size, and step count. SRA uses an EMA self-representation target, LayerSync directly aligns a selected internal pair, and LARA uses the shared layer-aware transition predictor.

\begin{table}[H]
    \centering
    \caption{\textbf{Comparison between SRA, LayerSync, and LARA.} Per-step profiling for self-contained internal alignment methods under the same \texttt{SiT-XL/2} TPU v4-16 setting.}
    \label{tab:appendix_sra_layersync_lara_compute}
    \scriptsize
    \setlength{\tabcolsep}{6pt}
    \renewcommand{\arraystretch}{0.9}
    \begin{tabular*}{0.78\textwidth}{@{\extracolsep{\fill}}lccc@{}}
        \toprule
        Method & FID $\downarrow$ & Wall-clock time/step $\downarrow$ & GFLOPs/step $\downarrow$ \\
        \midrule
        \texttt{SiT-XL/2}+SRA & 1.93 & \textbf{0.42} & \textbf{40,643} \\
        \texttt{SiT-XL/2}+LayerSync & 1.89 & \textbf{0.25} & \textbf{30,305} \\
        \texttt{SiT-XL/2}+LARA (Ours) & \textbf{1.82} & \textbf{0.30} & \textbf{35,342} \\
        \bottomrule
    \end{tabular*}
\end{table}

\subsection{Transition Pair and Predictor Design}
\label{app:transition_pair_predictor_design}

Layer-pair sampling replaces the fixed layer choice used by direct self-alignment. Very short gaps make transition prediction nearly local, while very long gaps can make the target state too distant early in training. Unless otherwise stated, main runs sample a mixture of short and medium gaps and keep the target range away from the final reconstruction-heavy blocks. The predictor is shared across valid pairs through layer and timestep conditioning, so the auxiliary branch learns reusable cross-depth transitions instead of separate pair-specific adapters.

\subsection{LARA Hyperparameters}
\label{app:realign_hyperparameters}

Each run reports the predictor architecture, layer-pair distribution, loss weights, diversity weight, cached-transition composition, EMA target rule, and training cost. Ablations use the same backbone and budget unless the table states otherwise.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{LARA hyperparameter setup.} Placeholder table for the architecture and loss settings used in main results and ablations.}
    \label{tab:appendix_realign_hparams}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lcccccccccc}
        \toprule
        Experiment & Predictor & Pair sampling & Time cond. & $\lambda_{\mathrm{pred}}$ & $\lambda_{\mathrm{cons}}$ & $\lambda_{\mathrm{div}}$ & Cached $a\to b$ & EMA comp. & Target detach & Extra cost \\
        \midrule
        Main ImageNet & \textbf{[TBD]} & \textbf{[TBD]} & \checkmark & \textbf{[TBD]} & \textbf{[TBD]} & \textbf{[TBD]} & \checkmark & \checkmark & \checkmark & \textbf{[TBD]} \\
        Component ablation & \textbf{[TBD]} & \textbf{[TBD]} & varies & \textbf{[TBD]} & varies & varies & varies & varies & \checkmark & \textbf{[TBD]} \\
        HumanML3D & \textbf{[TBD]} & \textbf{[TBD]} & \textbf{[TBD]} & \textbf{[TBD]} & \textbf{[TBD]} & \textbf{[TBD]} & \checkmark & \checkmark & \checkmark & \textbf{[TBD]} \\
        \bottomrule
    \end{tabular}}
\end{table}

\section{Experimental Protocols}
\label{app:experimental_details}
\label{app:baseline_protocol}

\subsection{Image Generation Experimental Details}
\label{app:image_generation_details}

\paragraph{Compute.}
We thank the TPU Research Cloud (TRC) program for providing TPU access. Main image-generation experiments are run on TPU v4-16 with batch size 256. Ablation studies are run on a local workstation with two NVIDIA RTX 4090 GPUs. The details of hyperparameters and sampler are provided in \Cref{tab:appendix_imagenet_protocol,tab:appendix_ablation_protocol}.

\paragraph{Existing assets.}
We use public research assets only for evaluation and baselines, including ImageNet, Tiny ImageNet, PASCAL VOC, HumanML3D, AMASS/SMPL, the Stable Diffusion VAE, DINOv2, SiT, and MDM~\citep{deng2009imagenet,everingham2010pascal,guo2022generating,mahmood2019amass,loper2015smpl,rombach2022ldm,oquab2023dinov2,ma2024sit,tevet2022mdm}. We cite the original sources and do not redistribute datasets or pretrained weights; use follows the official research terms and licenses of the corresponding assets.

\paragraph{Classification.}
We use the Tiny ImageNet dataset~\citep{deng2009imagenet}, upsample the images to $256\times256$, and train linear classification heads for 50 epochs. Performance is evaluated on the validation set.

\paragraph{Segmentation.}
For segmentation, we use the PASCAL VOC dataset~\citep{everingham2010pascal} and train linear heads for 25 epochs.

\paragraph{CKA.}
For CKA evaluations~\citep{kornblith2019similarity}, we use 4,000 samples from ImageNet $256\times256$.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Hyperparameter setup for main experiments.}}
    \label{tab:appendix_imagenet_protocol}
    \scriptsize
    \setlength{\tabcolsep}{5pt}
    \begin{tabular}{lcccc}
        \toprule
        & Table 1 (SiT-B) & Table 1 (SiT-L) & Table 1 (SiT-XL) & Table 2 \\
        \midrule
        \multicolumn{5}{l}{\textbf{Architecture}} \\
        Input dim. & $32\times32\times4$ & $32\times32\times4$ & $32\times32\times4$ & $32\times32\times4$ \\
        Num. layers & 12 & 24 & 28 & 28 \\
        Hidden dim. & 768 & 1024 & 1152 & 1152 \\
        Num. heads & 12 & 16 & 16 & 16 \\
        \midrule
        \multicolumn{5}{l}{\textbf{LayerSync}} \\
        $\lambda$ & 0.3 & 0.2 & 0.2 & 0.2 \\
        Syncing layers & (4,7) & (8,18) & (8,16) & (8,16) \\
        $\operatorname{sim}(\cdot,\cdot)$ & cos. sim. & cos. sim. & cos. sim. & cos. sim. \\
        \midrule
        \multicolumn{5}{l}{\textbf{Optimization}} \\
        Hardware & TPU v4-16 & TPU v4-16 & TPU v4-16 & TPU v4-16 \\
        Batch size & 256 & 256 & 256 & 256 \\
        Optimizer & AdamW & AdamW & AdamW & AdamW \\
        lr & 0.0001 & 0.0001 & 0.0001 & 0.0001 \\
        \midrule
        \multicolumn{5}{l}{\textbf{Interpolants}} \\
        $\alpha_t$ & $t$ & $t$ & $t$ & $t$ \\
        $\sigma_t$ & $1-t$ & $1-t$ & $1-t$ & $1-t$ \\
        Training objective & v-prediction & v-prediction & v-prediction & v-prediction \\
        Sampler & ODE Heun & ODE Heun & ODE Heun & SDE Euler--Maruyama \\
        Sampling steps & 250 & 250 & 250 & 250 \\
        Guidance & -- & -- & -- & 1.37 \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Hyperparameter setup for figures and ablation experiments.}}
    \label{tab:appendix_ablation_protocol}
    \scriptsize
    \setlength{\tabcolsep}{5pt}
    \begin{tabular}{lccc}
        \toprule
        & \Cref{fig:training_curve} (SiT-XL) & Table 6 (SiT-XL and SiT-B) & Table 7 (SiT-B) \\
        \midrule
        \multicolumn{4}{l}{\textbf{Architecture}} \\
        Input dim. & $32\times32\times4$ & $32\times32\times4$ & $32\times32\times4$ \\
        Num. layers & 28 & 28 / 12 & 12 \\
        Hidden dim. & 1152 & 1152 / 768 & 768 \\
        Num. heads & 16 & 16 / 12 & 12 \\
        \midrule
        \multicolumn{4}{l}{\textbf{LayerSync}} \\
        $\lambda$ & 0.2 & 0.3 & -- \\
        Alignment depth & (8,16) & -- & (2,8) \\
        $\operatorname{sim}(\cdot,\cdot)$ & cos. sim. & cos. sim. & cos. sim. \\
        \midrule
        \multicolumn{4}{l}{\textbf{Optimization}} \\
        Hardware & 2$\times$ RTX 4090 & 2$\times$ RTX 4090 & 2$\times$ RTX 4090 \\
        Training iteration & 400K & 400K & 400K \\
        Batch size & 256 & 256 & 256 \\
        Optimizer & AdamW & AdamW & AdamW \\
        lr & 0.0001 & 0.0001 & 0.0001 \\
        \midrule
        \multicolumn{4}{l}{\textbf{Interpolants}} \\
        $\alpha_t$ & $t$ & $t$ & $t$ \\
        $\sigma_t$ & $1-t$ & $1-t$ & $1-t$ \\
        Training objective & v-prediction & v-prediction & v-prediction \\
        Sampler & ODE Heun & ODE Heun & ODE Heun \\
        Sampling steps & 250 & 250 & 250 \\
        Guidance & -- & -- & -- \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Extended Related Work and Baseline Descriptions}
\label{app:baseline_descriptions}

In what follows, we summarize the main baseline methods used in our evaluation:

\begin{itemize}[leftmargin=*]
    \item \textbf{ADM}~\citep{dhariwal2021diffusion} builds upon U-Net-based diffusion models by introducing classifier-guided sampling, allowing fine-grained control over the trade-off between generation quality and diversity.
    \item \textbf{VDM++}~\citep{kingma2023understanding} proposes an adaptive noise schedule that adjusts dynamically during training, improving convergence and sample quality.
    \item \textbf{Simple diffusion}~\citep{hoogeboom2023simple} simplifies both the noise schedule and architectural components, enabling high-resolution image generation with improved computational efficiency.
    \item \textbf{CDM}~\citep{ho2022cascaded} introduces cascaded diffusion models that progressively refine images from low to high resolution using super-resolution stages, achieving better detail synthesis.
    \item \textbf{LDM}~\citep{rombach2022ldm} trains diffusion models in a compressed latent space learned by a VAE, drastically reducing training cost while maintaining image fidelity.
    \item \textbf{U-ViT}~\citep{bao2023all} combines ViT-based backbones with U-Net-style skip connections in the latent space, bridging the benefits of transformers and convolutional inductive biases.
    \item \textbf{DiffiT}~\citep{hatamizadeh2024diffit} enhances transformer-based diffusion models using time-aware multi-head self-attention, boosting sample efficiency and reducing training time.
    \item \textbf{MDTv2}~\citep{gao2023mdtv2} employs an asymmetric encoder-decoder transformer architecture with U-Net-inspired shortcuts in the encoder and dense skip connections in the decoder, improving generation quality and coherence.
    \item \textbf{MaskDiT}~\citep{zheng2023fast} introduces masked modeling into diffusion transformers by training with an auxiliary mask reconstruction objective, leading to better efficiency and generalization.
    \item \textbf{SD-DiT}~\citep{zhu2024sddit} builds on MaskDiT by incorporating a self-supervised discrimination objective using momentum encoding, enhancing the semantic richness of internal representations.
    \item \textbf{DiT}~\citep{peebles2023scalable} proposes a pure transformer architecture for diffusion, using AdaLN-zero modules to stabilize training and scale to large model sizes efficiently.
    \item \textbf{SiT}~\citep{ma2024sit} investigates the link between training efficiency and flow-based perspectives by transitioning from discrete-time diffusion to continuous flow matching, showing improved sample quality and convergence rates.
    \item \textbf{D-JEPA}~\citep{chen2024jepa} integrates Joint-Embedding Predictive Architectures into generative modeling by reframing masked image modeling as a generalized next-token prediction task, using diffusion or flow-matching losses to model per-token probability distributions in a continuous space.
    \item \textbf{VAR}~\citep{tian2024visual} redefines autoregressive image generation as a coarse-to-fine ``next-scale prediction'' process, diverging from standard raster-scan next-token prediction and allowing faster inference and scaling laws similar to large language models.
    \item \textbf{SRA}~\citep{jiang2025sra} is an EMA-based method that enables diffusion transformers to enhance their own representation learning and generation quality by aligning latent outputs from earlier, noisier layers with those from later, cleaner layers, eliminating the need for external guidance models.
\end{itemize}

\subsection{SiT Architecture Details}
\label{app:sit_architecture_details}

We use the same SiT-style transformer stack as the internal-alignment baselines~\citep{ma2024sit,haghighi2026layersync}. LARA keeps the backbone unchanged and adds only the training-time transition predictor described in \Cref{sec:method}.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{SiT backbone details.} Number of transformer layers, hidden dimensionality, and attention heads for the SiT models used in our experiments.}
    \label{tab:appendix_sit_architecture}
    \scriptsize
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{lccccc}
        \toprule
        Backbone & Layers & Hidden dim. & Heads & Patch size & Params \\
        \midrule
        \texttt{SiT-B/2} & 12 & 768 & 12 & 2 & 137M \\
        \texttt{SiT-L/2} & 24 & 1024 & 16 & 2 & 458M \\
        \texttt{SiT-XL/2} & 28 & 1152 & 16 & 2 & 675M \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{HumanML3D Setup}
\label{app:humanml3d_details}

\paragraph{Task.}
Given a sentence that describes a motion as a sequence of actions, the task is to generate a corresponding human motion. Each motion sequence consists of a series of human poses, where each pose is represented by 22 joints defined as 3D points in space.

\paragraph{Benchmark.}
We evaluate on HumanML3D~\citep{guo2022generating}, a standard text-to-motion benchmark built from motion-capture sequences and paired natural-language descriptions. To keep the comparison focused on the effect of internal alignment, all methods use the same motion representation, train/test split, text encoder, and evaluator as MDM~\citep{tevet2022mdm}. We therefore treat the HumanML3D preprocessing pipeline as fixed and vary only the internal training objective.

\paragraph{Implementation details.}
The motion backbone has eight transformer blocks and is trained for 600K iterations. LayerSync applies its direct hidden-state alignment to the selected internal pair $(3,6)$. LARA uses the same backbone and budget, but replaces direct matching with the transition predictor described in \Cref{sec:method}.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{HumanML3D protocol.} All rows use the same MDM data processing and evaluator; only the internal alignment objective changes.}
    \label{tab:appendix_humanml3d_setup}
    \small
    \setlength{\tabcolsep}{5pt}
    \begin{tabular}{lccccc}
        \toprule
        Method & Layers & Iter. & Internal supervision & Evaluator & Metrics \\
        \midrule
        MDM & 8 & 600K & none & Standard MDM & FID, R-Precision \\
        MDM + LayerSync & 8 & 600K & direct pair $(3,6)$ & Standard MDM & FID, R-Precision \\
        MDM + LARA (Ours) & 8 & 600K & transition predictor & Standard MDM & FID, R-Precision \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Metric and Measurement Definitions}
\label{app:metric_definitions}

\subsubsection{Protocol Map for Main Tables and Figures}

We follow the common appendix convention of recent internal-alignment work: the main paper keeps tables and figures compact, while the appendix states the measurement protocol, sample count, normalization, and citations. \Cref{tab:appendix_main_protocol_map} maps each main result to the corresponding measurement rule.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Protocol map for main tables and figures.} Each main artifact reports a compact result; the appendix specifies how it is measured.}
    \label{tab:appendix_main_protocol_map}
    \scriptsize
    \setlength{\tabcolsep}{2.5pt}
    \renewcommand{\arraystretch}{0.9}
    \begin{tabular*}{0.98\linewidth}{@{\extracolsep{\fill}}p{0.22\linewidth}p{0.29\linewidth}p{0.31\linewidth}p{0.11\linewidth}@{}}
        \toprule
        Main artifact & Quantity & Protocol & Citation \\
        \midrule
        \Cref{tab:imagenet_no_cfg,tab:imagenet_cfg} & FID, IS, precision, recall & Generate the stated number of samples with matched sampler, VAE, reference statistics, and CFG setting; use the same evaluator for all rows. & \citep{heusel2017gans,salimans2016improved,kynkaanniemi2019precision} \\
        \Cref{tab:humanml3d_results} & FID, R-Precision & Use the standard HumanML3D evaluator, split, text encoder, and motion representation for baseline, LayerSync, and LARA. & \citep{guo2022generating,tevet2022mdm} \\
        \Cref{tab:realign_ablation,tab:main_hyperparameter_tuning,tab:main_predictor_size_ablation} & FID-10K & Use SiT-B/2 with the same $300\mathrm{K}$ training budget, TPU setup, batch size, sampler, VAE, and reference statistics; only objective terms, timestep sampler, loss weights, or predictor size change. & \citep{heusel2017gans} \\
        \Cref{fig:projection_layout_observation} & PCA maps, z-score stable rank & Use frozen SiT-XL/2 hidden states for the same image over sampled timesteps; PCA is computed from channel-normalized token activations, and z-score stable rank is computed per layer from the same normalized feature matrix. & \citep{sanyal2020stable,daneshmand2020batchnormrankcollapse,feng2022rankdiminishing} \\
        \Cref{fig:cosine_squared_heatmap} & Layer cosine heatmap & Flatten activations over images and spatial tokens, normalize token features, average cosine similarity over sampled data and timesteps. & \citep{kornblith2019similarity,raghu2021visiontransformers} \\
        \Cref{fig:representation_analysis} & Linear probes, segmentation probe, DINOv2 CKA & Freeze backbone features, train lightweight probes per layer, and compute CKA to DINOv2 features on matched images and timesteps. & \citep{deng2009imagenet,everingham2010pascal,oquab2023dinov2,kornblith2019similarity} \\
        \bottomrule
    \end{tabular*}
\end{table}

Standard generation metrics follow the cited benchmark protocols. FID compares Gaussian approximations of real and generated Inception features,
\begin{equation}
\operatorname{FID}
=
\|\mu_r-\mu_g\|_2^2
+
\operatorname{Tr}\left(
\Sigma_r+\Sigma_g
-2(\Sigma_r\Sigma_g)^{1/2}
\right),
\end{equation}
where lower is better. IS is computed using the Inception classifier convention of \citet{salimans2016improved}. Precision and recall follow the improved generative-model support estimate of \citet{kynkaanniemi2019precision}. For HumanML3D, FID uses motion-evaluator embeddings and R-Precision reports whether the matching text appears in the top retrieved candidates under the standard HumanML3D/MDM evaluation protocol~\citep{guo2022generating,tevet2022mdm}.

For the curves in \Cref{fig:projection_layout_observation}, we use z-score stable rank: image and spatial tokens are flattened into a feature matrix, each channel is z-scored over the flattened token axis, and the resulting matrix $Z$ is summarized by $\|Z\|_F^2 / \|Z\|_2^2$ for each layer and timestep. Stable rank is commonly used as a soft-rank proxy in deep learning and as a diagnostic for representation collapse~\citep{sanyal2020stable,daneshmand2020batchnormrankcollapse,feng2022rankdiminishing}.

\section{Additional Ablations}
\label{app:realign_ablation_protocols}

The main ablation tables isolate the components needed for LARA. This appendix reports the broader sweeps that are useful for interpretation but too detailed for the main paper.

\subsection{Uniform Transition-Prediction Diagnostic}
\label{app:uniform_transition_diagnostic}

Before selecting the final layer-pair sampler, we run a diagnostic with uniform sampling over valid source--target pairs and uniform timesteps. We then group the predictor error by normalized layer gap, source-layer position, and timestep range. This diagnostic is used to choose the sampler qualitatively: adjacent layers tend to be easy but uninformative, very distant layers tend to be unstable, and medium gaps provide a stronger training signal.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Where uniform transition prediction struggles.} Prediction error is measured under uniform pair sampling and grouped by normalized layer-gap, source-layer, and timestep bins. Higher error marks transitions that are more difficult; useful bins are informative without being too unstable.}
    \label{tab:pair_sampling_ablation}
    \scriptsize
    \setlength{\tabcolsep}{2.2pt}
    \renewcommand{\arraystretch}{0.88}
    \begin{minipage}[t]{0.32\linewidth}
        \centering
        \textbf{Layer gap}\\[0.25em]
        \begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}lc@{}}
            \toprule
            Gap & $\mathcal{L}_{\mathrm{pred}}\downarrow$ \\
            \midrule
            0--25\% & 0.025 \\
            25--50\% & 0.039 \\
            50--75\% & 0.057 \\
            75--100\% & 0.152 \\
            \bottomrule
        \end{tabular*}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.32\linewidth}
        \centering
        \textbf{Source layer}\\[0.25em]
        \begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}lc@{}}
            \toprule
            Source & $\mathcal{L}_{\mathrm{pred}}\downarrow$ \\
            \midrule
            0--25\% & 0.060 \\
            25--50\% & 0.023 \\
            50--75\% & 0.027 \\
            75--100\% & 0.110 \\
            \bottomrule
        \end{tabular*}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.32\linewidth}
        \centering
        \textbf{Timestep}\\[0.25em]
        \begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}lc@{}}
            \toprule
            Range & $\mathcal{L}_{\mathrm{pred}}\downarrow$ \\
            \midrule
            0--25\% & -- \\
            25--50\% & -- \\
            50--75\% & -- \\
            75--100\% & -- \\
            \bottomrule
        \end{tabular*}
    \end{minipage}
\end{table}


\subsection{Compatibility with External Alignment}
\label{app:external_alignment_compatibility}

LARA is designed as an internal alignment signal, so it is natural to ask whether it can be added on top of an external representation-alignment method such as REPA~\citep{yu2024repa}. The two objectives supervise different relations: REPA anchors selected hidden states to features from an external visual encoder, while LARA learns transitions among the diffusion transformer's own layers. This control therefore tests complementarity rather than replacing one method with the other.

\begin{equation}
\mathcal{L}_{\mathrm{train}}
=
\mathcal{L}_{\mathrm{FM}}
+\lambda_{\mathrm{REPA}}\mathcal{L}_{\mathrm{REPA}}
+\lambda_{\mathrm{LARA}}\mathcal{L}_{\mathrm{LARA}}.
\end{equation}
All rows use the same backbone, data pipeline, optimizer, training length, and sampler. The REPA teacher and aligned layer follow the REPA protocol; LARA keeps the same predictor, layer-pair sampler, and loss weights as the internal-only run.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Compatibility with external representation alignment.} Appendix control for testing whether LARA can be combined with REPA-style external targets.}
    \label{tab:appendix_external_alignment_compatibility}
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular*}{0.92\linewidth}{@{\extracolsep{\fill}}lcccc@{}}
        \toprule
        Variant & External target & LARA & FID $\downarrow$ & Probe avg. $\uparrow$ \\
        \midrule
        SiT baseline & -- & -- & \textbf{[TBD]} & \textbf{[TBD]} \\
        REPA & DINOv2 & -- & \textbf{[TBD]} & \textbf{[TBD]} \\
        LARA (Ours) & -- & \checkmark & \textbf{[TBD]} & \textbf{[TBD]} \\
        REPA + LARA (Ours) & DINOv2 & \checkmark & \textbf{[TBD]} & \textbf{[TBD]} \\
        \bottomrule
    \end{tabular*}
\end{table}

If the combined row improves over both single-objective rows, the result indicates that external semantic anchoring and internal transition learning provide complementary supervision. If the gain is marginal, LARA should still be interpreted as the self-contained recipe used in the main experiments rather than as a required add-on to external alignment.

\subsection{Transition-Pair Selection}
\label{app:transition_pair_selection}

We sweep source and target blocks while keeping compute, batch size, and training iterations fixed. Generation metrics are reported together with transition-level metrics, since a low prediction loss can also indicate that the pair is easy.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Ablation on transition-pair selection.} Placeholder for testing source/target ranges and layer gaps.}
    \label{tab:appendix_pair_selection}
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{lccccc}
        \toprule
        Backbone & Source range & Target range & Gap & FID $\downarrow$ & Comp. error $\downarrow$ \\
        \midrule
        \textbf{[TBD]} & weak blocks & middle blocks & short & \textbf{[TBD]} & \textbf{[TBD]} \\
        \textbf{[TBD]} & weak blocks & middle blocks & medium & \textbf{[TBD]} & \textbf{[TBD]} \\
        \textbf{[TBD]} & weak blocks & decoder blocks & long & \textbf{[TBD]} & \textbf{[TBD]} \\
        \textbf{[TBD]} & random valid pairs & mixed & mixed & \textbf{[TBD]} & \textbf{[TBD]} \\
        \bottomrule
    \end{tabular}
\end{table}

\section{Optional Inference and Layer-Skip Diagnostics}
\label{app:optional_inference_diagnostics}

\subsection{Optional Transition-Interface Inference}
\label{app:optional_transition_inference}

The main paper uses the standard full-backbone sampler. The transition interface is evaluated only as an appendix diagnostic: at each Euler step, a route policy $\pi(k)$ either keeps the full backbone or replaces a block range $(a,b)$ with the predictor followed by the suffix blocks. Setting $\pi(k)=\mathrm{full}$ for all $k$ recovers standard Euler exactly.

\begin{algorithm}[!htbp]
\caption{\textbf{Optional transition-interface Euler sampler.}}
\label{alg:realign_inference}
\small
\begin{algorithmic}[1]
\Require schedule $1=t_K>\cdots>t_0=0$; condition $y$; backbone $v_\theta$ with prefix/suffix maps $F_{\theta,1:a},F_{\theta,b:L}$; predictor $G_\psi$; route policy $\pi(k)\in\{\mathrm{full}\}\cup\{(a,b):a<b\}$.
\State Sample $z_K\sim\mathcal{N}(0,I)$.
\For{$k=K,\ldots,1$}
    \State $r_k\gets\pi(k)$.
    \If{$r_k=\mathrm{full}$}
        \State $\hat v_k\gets v_\theta(z_k,t_k,y)$.
    \Else
        \State $(a,b)\gets r_k$.
        \State $A_a^{t_k}\gets F_{\theta,1:a}(z_k,t_k,y)$.
        \State $\widehat A_b^{t_k}\gets G_\psi(A_a^{t_k};e_a,e_b,e_{t_k})$.
        \State $\hat v_k\gets F_{\theta,b:L}(\widehat A_b^{t_k},t_k,y)$.
    \EndIf
    \State $z_{k-1}\gets z_k+(t_{k-1}-t_k)\hat v_k$.
\EndFor
\State \Return $z_0$
\end{algorithmic}
\end{algorithm}

Each diagnostic must report the skip rate, chosen layer jumps, and whether the same route is used at all timesteps or only at selected Euler steps. The comparison uses the same initial noise, class labels, schedule, and checkpoint as the full-backbone sampler.

\subsection{Magnitude-Calibrated Layer-Skip Diagnostic}
\label{app:magnitude_layer_skip}

This diagnostic tests whether a predicted target-layer state can replace skipped blocks. Because the cosine transition loss mainly controls direction, we attach a small magnitude head $h_\omega$ that predicts the target token scale from $\operatorname{sg}(\widehat A_{a\to b}^{\,t})$. Its loss is fully detached, so it updates only $h_\omega$ and never backpropagates into $v_\theta$ or $G_\psi$. At evaluation, we normalize the predicted direction, apply the predicted scale, and feed the calibrated state to the suffix $F_{\theta,b:L}$. This route is appendix-only and is not used for the main generation results.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Magnitude-calibrated layer skipping.} Appendix diagnostic for testing whether LARA's transition predictor can replace a skipped block range after training. All rows use the same checkpoint and sampler; only the internal route changes.}
    \label{tab:appendix_magnitude_layer_skip}
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular*}{0.94\linewidth}{@{\extracolsep{\fill}}lcccc@{}}
        \toprule
        Route & Jump $(a,b)$ & Magnitude head & FID $\downarrow$ & Rel. cost \\
        \midrule
        Full backbone & -- & -- & \textbf{[TBD]} & 1.00$\times$ \\
        Direction-only skip & \textbf{[TBD]} & off & \textbf{[TBD]} & \textbf{[TBD]} \\
        Calibrated short skip & \textbf{[TBD]} & on, stop-grad & \textbf{[TBD]} & \textbf{[TBD]} \\
        Calibrated medium skip & \textbf{[TBD]} & on, stop-grad & \textbf{[TBD]} & \textbf{[TBD]} \\
        \bottomrule
    \end{tabular*}
\end{table}

\section{Transition Predictor Configuration and Capacity}
\label{app:predictor_architecture_capacity}

We first specify how the transition predictor is attached to the SiT backbones, since the capacity sweep is only meaningful once the state geometry is fixed. LARA uses a compact ConvNet-style spatial predictor, implemented with ConvNeXt-style residual blocks~\citep{liu2022convnext}, on top of SiT hidden states. The latent tokens form a $16{\times}16$ grid at ImageNet $256{\times}256$, so lightweight local mixing can preserve the perceptual layout that matters for representation alignment, in the spirit of i-REPA~\citep{singh2025irepa}. The predictor takes a source-layer activation, injects source-layer, target-layer, and timestep conditioning, and returns an activation in the target-layer coordinate system. The target branch is not processed by the predictor; it is used as a stopped-gradient reference for the distance in \Cref{alg:lara_transition_distance}.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Transition predictor configuration for SiT.} The same LARA interface is used across the main ImageNet experiments; capacity controls vary the auxiliary parameter budget while keeping the conditioning and target interface fixed.}
    \label{tab:appendix_predictor_config}
    \scriptsize
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{0.9}
    \begin{tabular*}{0.98\linewidth}{@{\extracolsep{\fill}}lcccp{0.30\linewidth}p{0.18\linewidth}@{}}
        \toprule
        Backbone & Blocks & Hidden dim. & Token grid & Predictor state interface & Default budget \\
        \midrule
        SiT-B/2 & 12 & 768 & $16{\times}16$ & $A_a^t\in\mathbb{R}^{256\times768}\mapsto\widehat A_{a\to b}^{\,t}\in\mathbb{R}^{256\times768}$; condition on $(e_a,e_b,e_t,y)$; target $\operatorname{sg}(A_b^t)$ & 5--10\% of 137M \\
        SiT-L/2 & 24 & 1024 & $16{\times}16$ & $A_a^t\in\mathbb{R}^{256\times1024}\mapsto\widehat A_{a\to b}^{\,t}\in\mathbb{R}^{256\times1024}$; same conditioning and stopped target & 5--10\% of 458M \\
        SiT-XL/2 & 28 & 1152 & $16{\times}16$ & $A_a^t\in\mathbb{R}^{256\times1152}\mapsto\widehat A_{a\to b}^{\,t}\in\mathbb{R}^{256\times1152}$; same conditioning and stopped target & 5--10\% of 675M \\
        \bottomrule
    \end{tabular*}
\end{table}

\section{Representation and Transition Diagnostics}
\label{app:layer_statistics_projection}

This section collects the diagnostics behind \Cref{sec:representation_analysis} and the shared-structure motivation in \Cref{fig:projection_layout_observation}. The main paper reports the compact version; the appendix gives protocols, controls, and additional views.

\subsection{Diagnostic Protocol}
\label{app:diagnostic_protocol}

Diagnostics use the same checkpoints, dataset split, resolution, VAE, and layer sampling scheme as the main experiments. The comparison set is:
\begin{itemize}[leftmargin=*]
    \item baseline DiT/SiT backbone without auxiliary representation guidance;
    \item LayerSync-style direct cosine alignment;
    \item LayerSync-style alignment with a small projection head;
    \item pair-specific projectors for selected layer pairs;
    \item LARA with a shared conditional transition predictor.
\end{itemize}
For each method, report layer pairs, timestep ranges, projection or predictor architecture, loss weights, and whether an EMA target is used. Source groups are defined by block index for a backbone with $L$ blocks: $\mathcal{S}_{\mathrm{early}}=\{1,\dots,\lfloor0.2L\rfloor\}$, $\mathcal{S}_{\mathrm{mid}}=\{\lfloor0.4L\rfloor,\dots,\lfloor0.6L\rfloor\}$, and $\mathcal{S}_{\mathrm{late}}=\{\lceil0.8L\rceil,\dots,L\}$.

\subsection{Layerwise Probe Protocol}

For the representation analysis in \Cref{sec:representation_analysis}, we report probes across all backbone blocks. Linear classification uses frozen hidden states with a lightweight linear head. Segmentation probes use the same frozen features with a linear pixel classifier after spatial reshaping. CKA compares centered token activations with DINOv2 features on the same images and timesteps. Matched-FID checkpoints are preferred for the main figure so that the layerwise comparison reflects representation organization under comparable sample quality.

\subsection{Predictor-Output Diagnostics}

These diagnostics compare the predicted target state with the actual target layer across layer depth and denoising time. They show whether the transition predictor recovers useful target-layer structure alongside the scalar prediction loss.

\begin{figure}[!htbp]
    \centering
    \fbox{%
    \begin{minipage}[c][0.24\textheight][c]{0.9\linewidth}
        \centering
        \textbf{[PLACEHOLDER: predictor-output diagnostics]}\\[0.5em]
        Panels: PCA of actual target layer, PCA of predictor output, timestep PCA,\\
        predictor-target cosine heatmap, and layerwise CKA.
    \end{minipage}}
    \caption{\textbf{Predictor output versus target layer.} Placeholder for visualizing whether the learned transition recovers target-layer structure across depth and denoising time.}
    \label{fig:appendix_predictor_output_diagnostics}
\end{figure}

\subsection{Projection Capacity and Layer Gap}
\label{app:projection_capacity_layer_gap}

This ablation tests whether projection heads extract useful recoverable signal or simply absorb the auxiliary loss. The key comparison is direct alignment versus small and large projection heads across near and far layer pairs. A useful projection should improve generation and representation probes, along with local alignment loss.

\begin{figure}[!htbp]
    \centering
    \fbox{%
    \begin{minipage}[c][0.22\textheight][c]{0.9\linewidth}
        \centering
        \textbf{[PLACEHOLDER: projection capacity ablation]}\\[0.5em]
        Compare no projection, small projection, medium projection, and large projection.\\
        Report FID, alignment loss, representation probes, and training cost.
    \end{minipage}}
    \caption{\textbf{Projection capacity ablation.} Placeholder for testing whether projection capacity exposes useful signal across layer gaps.}
    \label{fig:appendix_projection_capacity}
\end{figure}

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Near vs. far layer alignment.} Placeholder for evaluating whether projection heads and LARA become more important as the layer gap increases.}
    \label{tab:appendix_near_far_alignment}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lccccc}
        \toprule
        Method & Layer gap & Projection / transition & FID $\downarrow$ & Alignment loss $\downarrow$ & Consistency error $\downarrow$ \\
        \midrule
        Direct alignment & Near & -- & \textbf{[TBD]} & \textbf{[TBD]} & -- \\
        Direct alignment & Far & -- & \textbf{[TBD]} & \textbf{[TBD]} & -- \\
        Projection-augmented alignment & Near & Small projector & \textbf{[TBD]} & \textbf{[TBD]} & -- \\
        Projection-augmented alignment & Far & Small projector & \textbf{[TBD]} & \textbf{[TBD]} & -- \\
        Pair-specific projector & Far & Per-pair projector & \textbf{[TBD]} & \textbf{[TBD]} & \textbf{[TBD]} \\
        LARA (Ours) & Near & Shared transition & \textbf{[TBD]} & \textbf{[TBD]} & \textbf{[TBD]} \\
        LARA (Ours) & Far & Shared transition & \textbf{[TBD]} & \textbf{[TBD]} & \textbf{[TBD]} \\
        \bottomrule
    \end{tabular}}
\end{table}

\subsection{Transition Generalization}
\label{app:transition_generalization}

This diagnostic distinguishes a shared transition interface from a collection of pair-specific heads. We train on a subset of layer pairs and evaluate prediction and composition error on held-out pairs at the same denoising timestep. Lower held-out and composition errors indicate that the shared predictor captures reusable cross-depth structure.

\begin{table}[!htbp]
    \centering
    \caption{\textbf{Held-out transition generalization.} Placeholder for testing whether LARA generalizes across unseen layer pairs better than pair-specific projectors.}
    \label{tab:appendix_transition_generalization}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lccc}
        \toprule
        Method & Held-out layer error $\downarrow$ & Composition error $\downarrow$ & FID $\downarrow$ \\
        \midrule
        Pair-specific projector & \textbf{[TBD]} & \textbf{[TBD]} & \textbf{[TBD]} \\
        Shared transition without consistency & \textbf{[TBD]} & \textbf{[TBD]} & \textbf{[TBD]} \\
        LARA shared transition (Ours) & \textbf{[TBD]} & \textbf{[TBD]} & \textbf{[TBD]} \\
        \bottomrule
    \end{tabular}}
\end{table}

\clearpage
\section{Qualitative Examples}
\label{app:qualitative_examples}

Class-conditional ImageNet samples from \texttt{SiT-XL/2} + LARA trained for $400\mathrm{K}$ iterations, sampled with CFG $=4$.

\newcommand{\laraqualclass}[2]{%
    \includegraphics[width=\textwidth,height=0.375\textheight,keepaspectratio]{#1}\\[-0.10em]
    \captionof{figure}{Generated samples from LARA at $400\mathrm{K}$ iterations with CFG $=4$. Class: ``#2''.}
}

\newcommand{\laraqualpage}[4]{%
    \begin{center}
    \captionsetup{font=small,justification=centering,singlelinecheck=false}
    \laraqualclass{#1}{#2}
    \end{center}
    \vfill
    \begin{center}
    \captionsetup{font=small,justification=centering,singlelinecheck=false}
    \laraqualclass{#3}{#4}
    \end{center}
    \clearpage
}

\laraqualpage{figures/qualitative/lara_400k_cfg4/sample_01.png}{goldfish}{figures/qualitative/lara_400k_cfg4/sample_02.png}{quail}
\laraqualpage{figures/qualitative/lara_400k_cfg4/sample_03.png}{golden retriever}{figures/qualitative/lara_400k_cfg4/sample_04.png}{tabby cat}
\laraqualpage{figures/qualitative/lara_400k_cfg4/sample_05.png}{tiger}{figures/qualitative/lara_400k_cfg4/sample_06.png}{giant panda}
\laraqualpage{figures/qualitative/lara_400k_cfg4/sample_07.png}{pickup truck}{figures/qualitative/lara_400k_cfg4/sample_08.png}{guacamole}
\laraqualpage{figures/qualitative/lara_400k_cfg4/sample_09.png}{cheeseburger}{figures/qualitative/lara_400k_cfg4/sample_10.png}{volcano}
\laraqualpage{figures/qualitative/lara_400k_cfg4/sample_11.png}{mantis}{figures/qualitative/lara_400k_cfg4/sample_12.png}{spindle}
\laraqualpage{figures/qualitative/lara_400k_cfg4/sample_13.png}{rattlesnake}{figures/qualitative/lara_400k_cfg4/sample_14.png}{Norwich terrier}
\laraqualpage{figures/qualitative/lara_400k_cfg4/sample_15.png}{hummingbird}{figures/qualitative/lara_400k_cfg4/sample_16.png}{coffee mug}
